<h1>Journal Entries</h1>
<hr>

<%= render partial: 'search' if current_user.posts.size > 0 %>


<%= button_to "Write a New Entry", new_post_path, method: :get %>

<% @posts.each do |post| %>
    <%= render partial: 'post', locals: { post: post } %>
<% end %>

<h2><%= "No journal entries!" if @posts.blank? %></h2>